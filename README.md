# Shengchao Lin's Personal Website

This repository hosts a plain static GitHub Pages site. There is no Ruby, Jekyll, npm, or build step required.

To preview locally:

```sh
python3 -m http.server 4173
```

Then open `http://127.0.0.1:4173/`.
